# API — discovery-service

> Em construção. Consulte a documentação técnica em [`docs/`](../../docs/) para
> o desenho completo dos serviços.

## Endpoints

*   `GET /` — Dashboard Eureka.
*   `GET /eureka/apps` — Lista de aplicações registradas (JSON/XML).
*   `GET /actuator/health` — Health check.